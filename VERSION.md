# LRenderDemo 版本维护

当前工程版本：**1.0**

## 版本定义

- CMake 内部版本：`1.0.0`
- 对外显示版本：`1.0`
- 场景文件格式版本：`1`
- Git 提交哈希是代码历史标识，不是工程版本号。

工程版本定义在根目录 `CMakeLists.txt` 的 `project(LRenderDemo VERSION ...)` 中。`README.md` 和本文件用于说明当前对外版本。

## 后续提交规则

每次提交代码都要同步更新工程版本：

1. 修复问题或小幅改动，递增补丁版本，例如 `1.0.0` 到 `1.0.1`。
2. 增加向后兼容功能，递增次版本，例如 `1.0.1` 到 `1.1.0`。
3. 出现不兼容的接口或文件格式变化，递增主版本，例如 `1.1.0` 到 `2.0.0`。
4. 同步更新 `README.md`、本文件和 `CHANGELOG.md`。
5. 提交前运行 VS2022 Debug 构建和 CTest。

场景格式版本只有在 `.lscene` JSON 结构发生兼容性变化时才递增，不随普通代码提交自动递增。
